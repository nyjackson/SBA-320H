import {Quote} from './Quote'
function AllQuotes() {
   // const result = quoteList.map((q) => <Quote key = {q._id}{...q}/>)
  return <div id="list-quotes">Quotes Page</div>;
}

export default AllQuotes;
