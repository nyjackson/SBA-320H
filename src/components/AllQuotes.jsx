import Quote from './Quote'
import {useEffect} from 'react'
function AllQuotes({dispatch}) {
   // const result = quoteList.map((q) => <Quote key = {q._id}{...q}/>)
   useEffect(() => {

   }, [])
  return <div id="list-quotes"><h1>Quotes Page</h1></div>;
}

export default AllQuotes;
